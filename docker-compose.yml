services:
  search-engine:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      SEARCH_ENGINE_DOCUMENT_DIR: /app/data/documents/
      SEARCH_ENGINE_INDEX_DIR: /app/data/index/
